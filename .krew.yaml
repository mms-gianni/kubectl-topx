apiVersion: krew.googlecontainertools.github.com/v1alpha2
kind: Plugin
metadata:
  name: topx
spec:
  version: {{ .TagName }}
  homepage: https://github.com/carafagi/kubectl-topx
  shortDescription: Monitor CPU and memory resources in real-time
  description: |
    kubectl-topx is a Kubernetes CLI tool for monitoring CPU and memory 
    resources (requests, limits, and actual usage) in real-time.
    
    Features:
    - Shows CPU and memory requests, limits, and current usage
    - Live updates with configurable refresh intervals
    - Progress bars for visual representation
    - Color-coded output based on usage level
    - Historical metrics with visual histograms
    
    Prerequisites:
    - Metrics Server must be installed in your Kubernetes cluster
  caveats: |
    This plugin requires the Metrics Server to be installed in your cluster.
    
    Install Metrics Server:
      kubectl apply -f https://github.com/kubernetes-sigs/metrics-server/releases/latest/download/components.yaml
    
    Usage:
      kubectl topx                    # Monitor current namespace
      kubectl topx -w                 # Display additional columns (requests, limits)
      kubectl topx -i 5                # Refresh every 5 seconds
      kubectl topx -A                 # Monitor all namespaces
      kubectl topx -n <namespace>     # Monitor specific namespace
      kubectl topx -t                 # Show historical metrics
  platforms:
  - selector:
      matchLabels:
        os: linux
        arch: amd64
    {{addURIAndSha "https://github.com/carafagi/kubectl-topx/releases/download/{{ .TagName }}/kubectl-topx-{{ .TagName }}-linux-amd64.tar.gz" .TagName }}
    bin: kubectl-topx
  - selector:
      matchLabels:
        os: darwin
        arch: amd64
    {{addURIAndSha "https://github.com/carafagi/kubectl-topx/releases/download/{{ .TagName }}/kubectl-topx-{{ .TagName }}-darwin-amd64.tar.gz" .TagName }}
    bin: kubectl-topx
  - selector:
      matchLabels:
        os: darwin
        arch: arm64
    {{addURIAndSha "https://github.com/carafagi/kubectl-topx/releases/download/{{ .TagName }}/kubectl-topx-{{ .TagName }}-darwin-arm64.tar.gz" .TagName }}
    bin: kubectl-topx
  - selector:
      matchLabels:
        os: windows
        arch: amd64
    {{addURIAndSha "https://github.com/carafagi/kubectl-topx/releases/download/{{ .TagName }}/kubectl-topx-{{ .TagName }}-windows-amd64.zip" .TagName }}
    bin: kubectl-topx.exe
